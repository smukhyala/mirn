{
  "name": "mirn",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "An interactive learning environment for perturbation in robotics.",
  "scripts": {
    "notes": "vite-node scripts/build-notes.ts",
    "measure": "vite-node scripts/measure-experiments.ts",
    "dev": "npm run notes && vite",
    "build": "npm run notes && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run test && npm run notes && npm run build"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/markdown-it": "^14.2.0",
    "@types/node": "^26.2.0",
    "fast-check": "^3.23.2",
    "gray-matter": "^4.0.3",
    "js-yaml": "^5.3.0",
    "katex": "^0.18.4",
    "markdown-it": "^15.0.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vitest": "^2.1.8"
  }
}
